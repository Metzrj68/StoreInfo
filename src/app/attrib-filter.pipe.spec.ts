import { AttribFilterPipe } from './attrib-filter.pipe';

describe('AttribFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AttribFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
